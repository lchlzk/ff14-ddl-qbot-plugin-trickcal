# 嘟嘟脸恶作剧插件 / Trickcal plugin

发送 `/tr` 查看目录。插件优先只读查询 GameKee 的嘟嘟脸恶作剧玩家 Wiki；GameKee 临时故障或缺少旧分类时，才使用 BWIKI 备用。无需 Token，不登录游戏，也不会读取玩家账号。

```text
/tr 角色 埃尔芬
/tr 角色 埃尔芬 国服
/tr 角色 埃尔芬 韩服
/tr 埃尔芬
/tr 神器 神器名（/tr 卡牌 同义）
/tr 宠物 名称
/tr 食物 名称
/tr 攻略 新手
/tr 兑换码 [国服|韩服]
/tr 搜索 关键词
/tr 随机角色 [国服|韩服]
/tr 蜡笔板 导入（发送后，再单独上传导出的 .json 文件）
/tr 蜡笔板 网页（群聊领取绑定码；私聊领取一次性网页登录链接）
/tr 蜡笔板 导入 取消
/tr 蜡笔板
/tr 蜡笔板 点亮 角色名或ID
/tr 蜡笔板 点亮 全部
/tr 蜡笔板 加点 角色名 1|2|3 攻击|防御|血量|暴击/暴伤|暴抗/暴伤抗
/tr 蜡笔板 加点 全部 1|2|3 攻击|防御|血量|暴击/暴伤|暴抗/暴伤抗
/tr 蜡笔板 撤销加点 角色名 1|2|3 百分比属性
/tr 蜡笔板 撤销加点 全部 1|2|3 百分比属性
/tr 蜡笔板 角色列表 [页码]
/tr 蜡笔板 角色 名称或ID（中文名支持简繁体及唯一单字译名差异）
/tr 蜡笔板 未点 攻击|防御 [页码]
/tr 蜡笔板 未点 血量|暴击/暴伤|暴抗/暴伤抗(防爆) [页码]
/tr 蜡笔板 未点 一层|二层|三层 属性 [页码]
/tr 蜡笔板 计划 [页码]
/tr 蜡笔板 删除 确认
```

角色列表、计划列表和“未点”查询在存在多页结果时，会显示仅查询者可点击的“上一页/下一页”按钮；角色详情会隐藏不存在的 `0/0` 属性，并为尚未点亮的百分比节点显示快捷加点按钮。按钮不可用的机器人账号或旧版 QQ 客户端仍可手动输入命令，不影响文字结果。

角色查询默认使用国服数据（GameKee 角色目录称“国际服”），末尾可指定 `国服` 或 `韩服`。国服没有该角色但韩服已收录时会自动使用韩服资料并明确提示。角色结果会生成一张 720 像素宽的纵向完整卡片，把角色形象首帧、喜欢的食物、蜡笔板全体加成和文字资料合在一张图中，并通过 QQ 官方临时图片存储与“随机角色”按钮放在同一条 Markdown 消息里；图片上传失败时会降级为同一条文字与按钮消息，不会拆出独立按钮。生成后的角色卡保存在 `data/cache/trickcal-cards`，7 天内跨重启复用；角色资料更新、区服不同或卡片格式升级时会生成新的缓存，旧缓存会自动过期并受 512 个文件、64 MiB 的双重上限约束。

Soshage 角色名称对照集中维护在嘟嘟脸插件仓库的 `src/qbot_trickcal/trickcal_role_aliases.json`。映射键为稳定的英文 `resource_name`，值为该角色在 Soshage、GameKee 及各区使用的简体、繁体和常用名称；例如 `Ner` 同时对应“宁琉/寧琉”和“涅尔/涅爾”。修改 JSON 后重新安装插件并重启机器人生效。角色查询只接受目录中的精确名称、明确映射或名称片段，不再用“只差一个汉字”猜测角色，以免把涅尔错误匹配为马尔。

兑换码默认查询国服，也可在末尾指定 `韩服`。机器人读取 GameKee 持续维护且明确分区的“最新兑换码”总表，只显示兑换码和过期时间，每服最多 10 个并自动去重；Wiki 没有填写截止时间时显示“未注明”，写明另行通知时显示“另行通知”。为避免混服，GameKee 故障时兑换码不会回退到不区分服务器的旧页面。

蜡笔板追踪兼容 [Soshage 收藏页](https://soshage.com/trickcal/collection#board) 导出的 collection v1 JSON。先发送 `/tr 蜡笔板 导入`，收到等待提示后，在 10 分钟内将下一条消息直接发送为一个 `.json` 文件；等待状态只绑定发起人及发起命令的当前群聊或私聊，下一条消息不是一个 `.json` 文件便会立即取消，也可发送 `/tr 蜡笔板 导入 取消` 手动退出。私聊和群聊都可以导入；群聊导入仍只覆盖发送者自己的记录，不会成为群共享数据，也不能读取或删除其他群友的记录。同一 QQ 用户可在其他会话主动查看自己的进度。不导入也能直接创建：第一次发送 `加点 角色名 层数 属性` 时，机器人会自动创建个人蜡笔板并点亮该角色；只想先记录拥有状态时也可使用 `点亮`。`点亮 全部` 会把当前公开目录内的所有角色记为已拥有，但不会点亮任何蜡笔节点；`加点 全部 层数 属性` 与对应的 `撤销加点` 只批量修改自己已经拥有、并且该层具有对应百分比节点的角色。属性写成 `全部` 时会处理该层所有百分比节点，例如 `/tr 蜡笔板 加点 全部 1 全部`。多个指定角色可以用英文逗号、中文逗号或顿号分隔，例如 `/tr 蜡笔板 加点 艾尔芬,艾雅 3 攻击`；名单会先完整校验再一次写入，最多 100 个角色。误操作可用 `撤销加点`。这些操作只改自己的记录；下次导入完整 JSON 会以文件内容覆盖手动修改。加成统计只计算百分比属性，固定数值属性不计入；总览和角色详情按第一、第二、第三层显示百分比攻击、防御、血量、暴击/暴伤、暴抗/暴伤抗五组节点的已点数及缺口。同一节点内的暴击与暴伤不重复计数，暴抗与暴伤抗同理。`未点` 查询覆盖所有已拥有角色，包括尚未开始蜡笔板的角色。机器人只保留角色拥有状态、已点节点、计划节点和数据更新时间，不保存 Soshage 账号、筛选器、卡牌、宠物或权重。上传文件上限为 512 KiB，格式、字段、数量、节点唯一性均会严格校验；删除必须明确输入“确认”。

个人管理网页与机器人共用同一份 SQLite 蜡笔板记录。私聊发送 `/tr 蜡笔板 网页` 可领取 10 分钟内有效且只能使用一次的登录链接；个体开发者机器人不能加好友时，也可在群聊领取网页地址和绑定码，按页面提示回到原群确认并创建独立的蜡笔板账号。账号登录会话保留 30 天，网页只持有不可逆的用户标识，不保存或返回 QQ 原始 OpenID。网页可以搜索简体、繁体或英文角色名，查看分层进度和累计百分比加成，逐项点亮/撤销节点，批量修改指定层、点亮全部角色，并导入或导出 Soshage JSON。所有写操作均校验当前登录会话及同源页面凭据。

登录网页后，可在右侧“账号与机器人”中点击“绑定新机器人”。页面会生成一条 10 分钟内有效的 `/tr 蜡笔板 绑定 绑定码` 命令；使用本人 QQ 到新机器人所在的群发送该命令后，新机器人、原机器人和网页即访问同一份蜡笔板。绑定码仅能使用一次且数据库只保存摘要；新机器人身份若已存在另一份蜡笔板或独立网页账号，系统会拒绝覆盖，需先导出备份并人工处理。

### 第三方只读 API

可分享的网页文档：`https://你的域名/tr-board/api-docs`（无需登录即可阅读）。生产环境地址为 [蜡笔板 API 文档](https://qbot-l.yizhixiaogame.com/tr-board/api-docs)。

已创建蜡笔板账号的用户可在网页右侧“第三方 API”生成只读令牌。令牌仅显示一次、有效期 90 天、最多同时保留 5 个；网页可查看用途与到期时间并随时撤销。数据库仅保存令牌的 SHA-256 摘要，令牌关联的是网页账号对应的共享蜡笔板，不需要用户真实 QQ 号或机器人管理后台账号。现阶段仅供第三方后端读取，**不能**通过 API 修改角色或节点；不要把令牌写入公开前端代码、链接参数或聊天消息。

请求时通过 HTTPS 的 `Authorization: Bearer <令牌>` 请求头传递。每个令牌最多 60 次/分钟，超过返回 `429` 与 `Retry-After`；令牌无效、过期或撤销后返回 `401`。不使用网页 Cookie，也不开放跨站浏览器脚本调用。

```sh
curl -H "Authorization: Bearer $TOKEN" https://你的域名/api/v1/tr-board/catalog
curl -H "Authorization: Bearer $TOKEN" https://你的域名/api/v1/tr-board/board
```

两个接口均返回 `{"ok":true,"data":...}`。`catalog` 的 `data.version` 为 `1`，包含公开角色目录、三层百分比节点 ID、分组、金币与金蜡笔；不包含任何用户拥有或加点状态。`board` 的 `data.version` 为 `1`，包含目录更新时间、当前账号的 `summary`、`layers` 和 `units`：`units[].owned` 是是否拥有，`units[].layers[].nodes[].selected` 是是否已点，`planned` 是计划节点，资源消耗与进度汇总以 `summary` 为准。不会返回 QQ OpenID、网页 Session 或 CSRF 值。第三方应按节点 ID 和 `version` 解析，不能假定角色显示名永不变化。

服务器默认页面地址是 `http://127.0.0.1:8080/tr-board/`，仅供宿主机测试。要让群友访问，请通过 Caddy、Nginx 或云负载均衡为机器人 `8080` 端口提供公网 HTTPS，并在 `.env` 中设置：

```text
TRICKCAL_WEB_PUBLIC_URL=https://你的域名/tr-board/
TRICKCAL_WEB_SECURE_COOKIE=true
```

域名应加入 QQ 机器人平台允许发送的可信域名；修改 `.env` 后执行 `bot.cmd restart`（Linux：`./bot.sh restart`）。不要把 `8080` 端口直接暴露到公网 HTTP。

蜡笔板回复中的中文角色名统一显示为简体，并保留英文名便于核对。`/tr 蜡笔板 未拥有 [页码]`（也可写 `未点亮`）列出公开目录中尚未登记为拥有的角色；`/tr 蜡笔板 取消点亮 角色名或逗号名单`（也可写 `反点`）移除指定拥有角色，并同步清除它们的已点与计划节点。清空全部拥有角色必须明确发送 `/tr 蜡笔板 取消点亮 全部 确认`。

总览、角色列表、角色详情和计划页中的节点数、进度角色数、金币合计都只计算百分比节点，普通固定数值节点不会混入；同时会根据 Soshage 节点材料 ID `610004` 统计已使用及计划需要的金蜡笔数量。公开目录合并 Soshage 与 [Crayon-note](https://github.com/kai2002002-crayon/Crayon-note)：同一角色优先保留 Soshage 的精确节点、金币及材料，Crayon-note 的 `data_core.js` 用来补充缺失角色及其三层百分比路径；补充节点的百分比与金蜡笔数量取自 Crayon-note，金币采用 Soshage 当前同层节点的通用成本。机器人固定在每天 18:00（UTC+8）刷新一次，并将精简后的目录缓存在 `data/cache/trickcal-board/catalog-v4.json`；首次使用或发现旧版缓存时会即时获取。Crayon-note 不可用或格式校验失败时会继续使用 Soshage，Soshage 更新失败则保留旧缓存并每 10 分钟重试，不会影响个人记录。导入或手动维护的个人进度保存在 SQLite 数据卷中，不会由公开目录定时任务自动修改，也不进入一键安装包。

角色、神器、宠物、攻略、兑换码及搜索优先使用 GameKee 当前内容；食物分类目前在 GameKee 没有可逐项读取的公开目录，因此暂用 BWIKI。资料结果通常会显示实际来源及更新时间或修订号；兑换码为保持简洁，只显示区服、兑换码和过期时间。两边都是玩家维护资料，并非游戏官方接口，可能存在延迟、缺页或编辑错误。`/tr` 使用本群工具箱日限额；帮助不计次数。管理员可执行 `/command disable tr` 或 `/command enable tr`。

插件仅访问固定的 `www.gamekee.com` 匿名只读端点、BWIKI MediaWiki API、Soshage 页面公开使用的只读角色/节点目录，以及 GitHub API 中固定仓库的 `data_core.js`，并设置响应大小限制、严格字段解析、连接超时、同请求合并及有界缓存；不会执行远程 JavaScript。遇到安全验证、限流或维护时不会绕过防护，也不会改用游戏登录接口。

Character lookups default to the international/Chinese release data and accept an explicit `国服` or `韩服` suffix. Missing Chinese-release entries fall back to Korean data with a visible notice. A compact image card shows character art, favorite foods and crayon-board bonuses. The plugin reads GameKee's anonymous public Wiki data first and uses the BWIKI MediaWiki API only as a fallback. It does not use game credentials or player-account APIs. Results are bounded, rate-limited and cached, and clearly identify the community-maintained source.
